import React from "react";

const Graphs = () => {
  return (
    <div className="graphs">
      <div className="graph-card">Money In vs Money Out (placeholder)</div>
      <div className="graph-card">Pending Payments Trend (placeholder)</div>
    </div>
  );
};

export default Graphs;
